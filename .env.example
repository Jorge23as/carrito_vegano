# Copiar este archivo a .env y completar con valores reales.
# .env NUNCA se sube a GitHub (ver .gitignore) porque contiene credenciales.
SECRET_KEY=generar-una-nueva-con-get_random_secret_key
DEBUG=True

DB_NAME=tienda_multitenant
DB_USER=tienda_app
DB_PASSWORD=cambiar-esta-clave
DB_HOST=127.0.0.1
DB_PORT=3306
